{
  "name": "ramen",
  "version": "0.0.1",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "esbuild --bundle --sourcemap --outdir=dist ./src/index.js",
    "build:production": "esbuild --bundle --sourcemap --minify --outdir=dist ./src/index.js",
    "dev": "miniflare --live-reload --watch --debug",
    "publish": "wrangler publish dist/index.js"
  },
  "devDependencies": {
    "miniflare": "^2.2.0",
    "wrangler": "^0.0.15"
  },
  "dependencies": {
    "hono": "^0.3.6"
  }
}