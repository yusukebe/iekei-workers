{
  "name": "ramen",
  "version": "0.0.1",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "esbuild --bundle --sourcemap --outdir=dist ./src/index.tsx",
    "build:production": "esbuild --bundle --sourcemap --minify --outdir=dist ./src/index.tsx",
    "dev": "miniflare --live-reload --watch --debug",
    "publish": "wrangler publish dist/index.js",
    "test": "NODE_OPTIONS=--experimental-vm-modules jest --verbose"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^3.3.1",
    "@types/jest": "^27.4.0",
    "@types/react": "^17.0.39",
    "@types/react-dom": "^17.0.11",
    "@types/react-helmet": "^6.1.5",
    "esbuild-jest": "^0.5.0",
    "jest": "^27.5.0",
    "jest-environment-miniflare": "^2.2.0",
    "miniflare": "^2.2.0",
    "wrangler": "^0.0.15"
  },
  "dependencies": {
    "hono": "^0.3.7",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-helmet": "^6.1.0"
  }
}
