{
  "name": "ramen",
  "version": "0.0.1",
  "main": "dist/server.js",
  "license": "MIT",
  "scripts": {
    "build": "esbuild --bundle --sourcemap --outdir=dist ./src/server.tsx",
    "build:production": "esbuild --bundle --sourcemap --minify --outdir=dist ./src/server.tsx",
    "dev": "miniflare --live-reload --watch --debug",
    "publish": "wrangler publish dist/server.js"
  },
  "devDependencies": {
    "@types/react": "^17.0.39",
    "@types/react-dom": "^17.0.11",
    "@types/react-helmet": "^6.1.5",
    "miniflare": "^2.2.0",
    "wrangler": "^0.0.15"
  },
  "dependencies": {
    "hono": "^0.3.6",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-helmet": "^6.1.0"
  }
}
